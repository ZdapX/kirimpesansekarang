<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background-color: #000; color: white; }</style>
</head>
<body class="flex flex-col items-center pt-20 px-6">
    <div class="w-full max-w-md bg-[#121212] border border-[#1f1f1f] p-8 rounded-[40px] text-center">
        <div class="w-20 h-20 bg-cyan-500 rounded-full mx-auto mb-4 flex items-center justify-center text-3xl">👤</div>
        <h2 class="text-xl font-bold mb-6">Kirim pesan anonim ke @<%= target %></h2>
        
        <form action="/send/<%= target %>" method="POST">
            <textarea name="text" required 
                class="w-full h-40 p-5 rounded-3xl bg-[#0a0a0a] border border-[#222] text-white focus:ring-2 ring-cyan-500 outline-none resize-none mb-4"
                placeholder="Tulis sesuatu yang jujur..."></textarea>
            
            <button class="w-full py-5 bg-white text-black rounded-full font-black text-lg shadow-lg hover:bg-cyan-400 transition">
                KIRIM SEKARANG!
            </button>
        </form>
        <p class="mt-8 text-[10px] text-gray-600 uppercase tracking-[0.2em]">Pesan ini anonim. Aman.</p>
    </div>
</body>
</html>
